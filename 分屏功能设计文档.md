# 分屏功能设计文档

> 本文档用于记录"文档编辑器 + 带团板"分屏功能的设计决策，随讨论逐步更新。
> 创建时间：2026-07-21
> 状态：设计决策已完成（13 项原始决策 + 10 项复审问题决策全部确认），待落地路径细化与实现

---

## 一、需求背景

当前应用有两个独立页面：**文档编辑器**和**带团板**，通过按钮来回切换，操作链路长。希望在分屏模式下：左半边文档编辑器，右半边带团板，并简化"选中文本 → 入库 → 落便签"的工作流。

### 当前工作流（待简化）
1. 文档编辑器选中文本 → 添加到对应类型数据库
2. 切换到带团板
3. 点开数据库面板
4. 点选条目 → 作为便签添加到当前流程单元画布

### 期望工作流（分屏下）
1. 文档编辑器选中文本 → 添加到数据库
2. **同步**在带团板当前流程单元画布落一个便签

---

## 二、系统现状摘要（事实）

- **页面切换**：`UIRender.switchPage(page)` 用单一 `AppState.currentPage` + `.page-section.active` 切换，全局只有一个活动页面。参考 [index.html#L12594-L12646](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L12594-L12646)
- **带团板结构**：`board-viewport` → `board-track`（横向流程单元轨道）→ `flow-unit` → `flow-unit-canvas`（便签画布）。流程单元横向切换，**不是二维无限画布**。便签在画布内绝对定位。
- **鸟瞰图**：`board-minimap` 是 `position:fixed; left:16px` 浮窗（[L7013](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L7013)）；底部还有 `board-thumbnails` 流程单元导航。
- **数据库面板**：`boardDbPanel` 在带团板右侧。
- **隐式假设**：`AppState.currentPage`、`BoardManager`/`DocEditor`/`WorldClock` 单例、navbar 标题、保存指示器都假设"同一时刻只有一个页面在显示"。

---

## 三、候选方案对比

| 方案 | 优点 | 缺点 | 适用 |
|---|---|---|---|
| A. CSS 双栏同 DOM | 复用现有逻辑 | 事件/坐标互相污染，需全面审计 | 原型验证 |
| B. iframe 分屏 | JS 上下文隔离，带团板内部 0 改动 | 内存翻倍，跨 iframe 通信成本 | **推荐起步** |
| C. 组件化重构 | 长远最优雅 | 等于重写 | 长远方向 |

**当前推荐**：方案 B 起步，方案 C 作为长远方向。

> **决策 1（2026-07-21）**：采用方案 B（iframe 分屏）作为起步方案。
> 理由：JS 上下文隔离可使带团板内部交互（拖动/缩放/框选/鸟瞰图）几乎零改动，降低坐标错位风险；跨 iframe 通信成本可通过 postMessage 桥接消化。

---

## 四、已识别的技术风险

### 坐标错位问题根因
1. `getBoundingClientRect()` 隐式假设画布占满视口左侧，分屏后画布左上角不再是 `(0,0)`。
2. 鸟瞰图 `position:fixed` 位置可能挡住文档编辑器，视窗框比例映射依赖主视口可见范围。
3. 框选矩形若监听 `document`/`window`，鼠标移到文档编辑器半边会跟过去。
4. 拖拽画布（pan）同上。
5. 便签拖动/缩放手柄拖到另一半屏时事件可能丢失。

### 修复原则
- 把所有"画布交互"事件监听从 `document`/`window` 下沉到 `board-viewport`。
- 用 `viewport.getBoundingClientRect()` 作为坐标基准。
- 拖动时使用 `setPointerCapture` 防止事件丢失。
- **该改造不分屏也应做，是健壮性提升。**

---

## 五、工作流简化设计（草稿）

- **触发点**：文档编辑器选中文本 → 弹"添加到数据库"菜单 → 选类型后同时执行：
  1. 写入 `AppState.currentModule.databases[group][type]`
  2. 调用 `BoardManager.addNoteFromDbEntry(entry)` 落便签
- **便签落点**：默认画布中心或上一次落点附近，避免叠加。
- **修饰键**：Shift 表示"只入库不出便签"，保留原流程。
- **关联**：便签通过 `dbEntryId` 关联数据库条目，便于后续同步。

---

## 六、边界与冲突处理（草稿）

| 场景 | 处理（待确认） |
|---|---|
| 分屏下打开战斗模块/角色图鉴 | 全屏覆盖，退出后回到分屏 |
| 分屏下模块切换/退出模块 | 退出模块自动关闭分屏 |
| 世界时钟 | 绑定到带团板那一栏 |
| navbar/保存指示器 | 顶部栏保持单一 |
| 拖动便签鼠标跑到另一半屏 | `setPointerCapture` |
| 文档选中文本被遮挡 | 分割条 + `pointer-events` 隔离 |
| 切换流程单元 | 只影响带团板那一栏 |
| 分屏比例记忆 | `localStorage` 存储 |

---

## 七、落地路径（细化版）

> 状态：2026-07-21 开始实施

### 阶段一：坐标系统健壮化（不分屏，先改底盘）
**目标**：在现有单页模式下完成 board 内部交互的健壮化改造，为 iframe 拆分打基础。

**任务清单**：
1. board 拖拽交互从 mouse 事件改为 PointerEvent + `setPointerCapture`（Q1）
   - 画布平移（pan）
   - 框选（selection box）
   - 便签拖动
   - 便签缩放手柄
   - 连线模式是点击式，无需改造（Q29 已核实）
2. window resize 补 `renderMinimap` + `renderConnections`（Q5/Q24）
3. 移除 board 内 `currentPage !== 'board'` 守卫（Q7）
4. 验证：所有拖拽操作鼠标移出视口仍能正常结束；窗口缩放后鸟瞰图和连线正确

**验收标准**：单页模式下所有 board 交互正常，无回归问题。

---

### 阶段二：页面拆分（shared.js + 三个 html）
**目标**：完成物理拆分，主窗口为壳，两个 iframe 可独立加载运行（单页模式）。

**任务清单**：
1. 提取 `shared.js`（Q8）
   - `generateUUID()`
   - `App.showConfirm/hideConfirm/confirmOk` + `confirmOverlay` 逻辑（Q26）
   - postMessage 通信桥基础（sendMessage / onMessage 工具函数）
   - 通用工具函数
2. 创建 `board.html`（从 index.html 拆分出带团板相关 HTML/CSS/JS）
   - board-toolbar、board-viewport、board-minimap、board-thumbnails、world-clock、board-db-panel、board-search-bar 等 DOM
   - BoardManager 全部逻辑
   - `confirmOverlay` DOM + 样式（Q26）
   - 战斗模块/角色图鉴相关留在主窗口（决策7&8）
3. 创建 `doc-editor.html`（从 index.html 拆分出文档编辑器）
   - 文档编辑器 DOM + DocEditor 全部逻辑
   - 文档右键菜单、格式化工具栏等
   - `confirmOverlay` DOM + 样式（Q26）
4. 改造 `index.html` 为主窗口壳
   - navbar、保存指示器（决策9）
   - 战斗模块、角色图鉴页面（决策7&8）
   - iframe 容器（初始单页模式加载对应 iframe）
   - 分屏布局容器（暂不启用）
5. navbar 双向页面切换（文档↔带团板），分屏时隐藏（Q10/Q9）

**验收标准**：单页模式下文档编辑器和带团板功能完整，与拆分前行为一致。分屏按钮存在但暂不启用。

---

### 阶段三：跨 iframe 通信桥 + 状态同步
**目标**：建立主窗口真相源，所有共享状态通过 postMessage 同步。

**任务清单**：
1. 定义 postMessage 消息协议（Q13）
   - `MODULE_CHANGED`（主窗口→iframe，带 module 数据）
   - `DOC_UPDATED`（doc-editor→主窗口，防抖推送 document）
   - `BOARD_UPDATED`（board→主窗口，防抖推送 board）
   - `DB_ENTRY_ADDED`（doc-editor→主窗口→board 广播，带 entry）
   - `REQUEST_MODULE`（iframe→主窗口，请求当前模块数据）
   - `READY`（iframe→主窗口，通知加载完成）
   - `JUMP_TO_SOURCE`（board→主窗口→doc-editor，带 entryId）
   - `OPEN_CHARACTER_GUIDE`（board→主窗口，带 characterId）
2. 主窗口 AppState 共享状态管理（决策4/Q3）
   - 持有 currentModule 的 document / board / databases 副本
   - 接收 iframe push 更新并合并
   - 模块切换时广播 MODULE_CHANGED
3. doc-editor 接入
   - 初始化时请求模块数据
   - 入库逻辑改本地生成 id（Q2）+ 发 DB_ENTRY_ADDED
   - 文档编辑防抖推送 DOC_UPDATED
   - 接收 JUMP_TO_SOURCE 并定位高亮（Q14）
4. board 接入
   - 初始化时请求模块数据
   - 便签/连线编辑防抖推送 BOARD_UPDATED
   - 接收 DB_ENTRY_ADDED 刷新数据库面板（Q6）
   - 跳转角色图鉴发 OPEN_CHARACTER_GUIDE（Q15）

**验收标准**：单页模式下所有功能正常，状态通过 postMessage 同步。打开开发者工具可观察到消息流转。

---

### 阶段四：工作流简化（入库即落便签）
**目标**：实现分屏核心价值——入库即落便签 + 跳转原文联动。

**任务清单**：
1. 「入库即落便签」开关（决策5）
   - navbar 增加开关按钮/设置项
   - 默认开启
2. board 收到 DB_ENTRY_ADDED 后落便签（决策6）
   - 当前激活流程单元
   - 上次落点附近错开避免叠加
   - 便签通过 dbEntryId 关联数据库条目
3. 「跳转至原文」跨 iframe 定位（Q14）
   - board → 主窗口 → doc-editor
   - doc-editor 滚动到 source-marker + 高亮闪烁 + 面包屑
   - 分屏下不退出分屏；单页模式仍切页

**验收标准**：分屏下选中文本→入库→带团板自动落便签，点便签跳转原文→左栏定位高亮。

---

### 阶段五：分屏模式 + 边界完善
**目标**：启用分屏布局，完善所有边界场景。

**任务清单**：
1. 分屏布局与切换（决策2）
   - navbar 分屏/退出分屏按钮
   - 左右 iframe 两栏布局
   - localStorage 记忆分屏状态
   - 退出分屏回到触发前页面
2. 分割条拖动（决策11）
   - 可拖动调整比例
   - localStorage 记忆比例
   - 拖动期间 board resize 正常（Q24/Q25 已验证）
3. 战斗模块/角色图鉴全屏覆盖（决策7&8/Q15）
   - 分屏下打开 = 主窗口切页隐藏分屏区
   - 退出回到分屏状态
4. 模块切换广播（Q4）
   - 主窗口统筹模块切换
   - 广播给所有已加载 iframe
5. 世界时钟归带团板栏（决策10）
   - 已在 board.html 内，确认即可
6. 回归测试：所有边界场景验证

**验收标准**：分屏功能完整可用，所有边界场景正确处理，无明显 bug。

---

## 八、待定决策清单（随讨论更新）

| # | 决策项 | 状态 | 当前倾向 |
|---|---|---|---|
| 1 | 架构方案：iframe vs 单 DOM vs 组件化 | **已定：iframe** | — |
| 2 | 分屏触发方式（按钮/快捷键）与可关闭 | **已定：navbar 切换按钮** | — |
| 3 | iframe 加载内容形式（拆分 index.html vs 复用同页） | **已定：拆分独立页** | — |
| 4 | 共享状态（AppState/模块/数据库）同步策略 | **已定：主窗口为真相源** | — |
| 5 | "入库即落便签"默认开启 vs 开关 | **已定：独立开关默认开** | — |
| 6 | 便签落点规则（当前流程单元？落点策略？） | **已定：当前单元+错开** | — |
| 7 | 战斗模块/角色图鉴在分屏下是否全屏覆盖 | **已定：全屏覆盖（留主窗口）** | — |
| 8 | 全屏覆盖层归属（主窗口 vs iframe 内） | **已定：主窗口切页** | — |
| 9 | navbar/保存指示器归属 | **已定：主窗口顶部统一栏** | — |
| 10 | 世界时钟归属 | **已定：带团板栏（右）** | — |
| 11 | 分割条是否可拖动调整比例 + 记忆 | **已定：可拖动 + localStorage 记忆** | — |
| 12 | 鸟瞰图/缩略图在 iframe 内的定位 | **已定：保持 board iframe 内 fixed** | — |
| 13 | 跨 iframe 通信机制（postMessage vs Electron IPC） | **已定：postMessage** | — |

---

## 九、决策记录（随讨论追加）

> 每确认一项决策，在此追加日期 + 决策内容 + 理由。

- **2026-07-21 决策 1**：采用 iframe 分屏方案。理由：JS 上下文隔离使带团板交互零改动，降低坐标错位风险。
- **2026-07-21 决策 3**：iframe 加载拆分后的独立页（doc-editor.html / board.html），主窗口 index.html 作为分屏壳。理由：彻底隔离、避免全量代码双加载与单例冲突，契合 iframe 方案初衷，且利于长远组件化。
- **2026-07-21 决策 4**：共享状态采用"主窗口为真相源"。主窗口持有 AppState 的共享部分（模块、数据库），文档内容归 doc-editor 本地、便签布局归 board 本地；iframe 通过 postMessage 请求/广播。理由：单一真相源一致性最强，为入库即落便签提供天然事件桥。代价：DocEditor 入库、BoardManager 读库需改造成消息通信。
- **2026-07-21 决策 2**：分屏触发为 navbar 顶部"分屏/退出分屏"切换按钮，退出回到触发前页面，启动默认单页，用 localStorage 记忆是否分屏。不加快捷键。理由：入口统一、可关闭、保持现状启动习惯。
- **2026-07-21 决策 5**：「入库即落便签」为独立开关，默认开。理由：默认便利同时保留用户控制权，关闭后退化为原流程。
- **2026-07-21 决策 6**：便签落在当前激活流程单元，按上次落点附近错开避免叠加；落点策略由 board iframe 本地维护。理由：避免叠加、解耦主窗口与 board 布局。
- **2026-07-21 决策 7&8**：战斗模块、角色图鉴代码留在主窗口 index.html，分屏下打开 = 主窗口切页隐藏分屏区全屏显示，退出回分屏。理由：这两块与主窗口状态交互多，拆 iframe 成本高；留主窗口零拆分成本且全屏覆盖自然实现。
- **2026-07-21 决策 9**：navbar、保存指示器归主窗口顶部统一栏，两个 iframe 不重复。理由：避免重复、保存状态全局可见。
- **2026-07-21 决策 10**：世界时钟归带团板栏（右）。理由：世界时钟与跑团流程关联强。
- **2026-07-21 决策 11**：分割条可拖动调整比例 + localStorage 记忆。理由：灵活适应用户偏好。**关键风险**：拖动改变 board iframe 宽度，须确保画布坐标全部基于 `board-viewport.getBoundingClientRect()` 实时基准（见第四章），并监听 resize 重算。
- **2026-07-21 决策 12**：鸟瞰图/缩略图保持 board iframe 内 `position:fixed`。理由：board.html 独立页，fixed 相对 iframe 视口，鸟瞰图天然只在右半边，无需额外处理。
- **2026-07-21 决策 13**：跨 iframe 通信用 `window.postMessage`。理由：iframe 间直接通信，不走主进程绕路，高效简单。

---

## 十、代码复审发现（2026-07-21）

### 利好（无需大改的现有设计）
- **坐标基准已基于 `canvas.getBoundingClientRect()`**：框选([L17654](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L17654))、滚轮缩放([L17706](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L17706))、便签拖拽([L21705](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L21705)/[L21797](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L21797))、框选更新([L21757](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L21757)) 均用 canvas 相对坐标。iframe resize 后 `getBoundingClientRect()` 返回新值，坐标自动正确。
- **虚拟坐标系** `_screenToVirtual`/`_virtualToScreen`（[L17844](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L17844)）基于 canvas 相对坐标，与 iframe 位置无关。
- **鸟瞰图比例映射**基于 `unitCanvasEl.parentElement.clientWidth/clientHeight`（[L18055](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L18055)），与 iframe 位置无关，resize 后重算即可。
- **世界时钟**本就在带团板页（[L10628](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L10628)，WorldClock 定义 [L29481](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L29481)，init [L17452](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L17452)），移入 board.html 自然。
- **数据库面板** `board-db-panel`（[L6173](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L6173)）属 board，迁移自然。
- **document 事件监听有 `AppState.currentPage !== 'board'` 守卫**（[L17631](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L17631)/[L17668](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L17668)/[L17679](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L17679)），iframe 隔离后 board document 不会跨到 doc-editor。

### 待决策的额外问题（第一轮）
| # | 问题 | 现状 | 影响 |
|---|---|---|---|
| Q1 | 拖拽移出 board iframe 边界 mouseup 丢失 | 拖拽靠 board document mousemove/mouseup，无 setPointerCapture | 鼠标拖到左半边时拖拽卡住 |
| Q2 | 入库 entry.id 生成时机 | 现直接写 AppState 并立即用 entry.id 包 source-marker | postMessage 异步，source-marker 需 id，存在同步往返 |
| Q3 | 文档内容(document)归属与保存汇总 | AppState.currentModule.document 是模块数据一部分 | 决策4说文档归 doc-editor 本地，但保存时谁汇总进存储？ |
| Q4 | 模块切换广播协议 | 切模块时文档/数据库/便签都重载 | 需主窗口广播给 doc-editor(重载文档) + board(重载便签) |
| Q5 | board iframe resize 时鸟瞰图不重渲染 | window resize 只重定位 alerts-bar([L17459](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L17459))，未调 renderMinimap | 拖分割条后鸟瞰图视窗框比例不准 |
| Q6 | 数据库面板刷新 | 入库后需 board 数据库面板显示新条目 | 需 DB_ENTRY_ADDED 广播让 board 刷新面板 |
| Q7 | AppState.currentPage 守卫是否保留 | board.html 独立页内 currentPage 恒为 board | 守卫可移除，但需确认监听不会被非 board 场景误调 |

### 复审问题决策（第一轮）
- **2026-07-21 Q1**：采用方案 A，board 拖拽改用 PointerEvent + `setPointerCapture`。理由：capture 后鼠标移出 board iframe 仍持续收到 pointermove/pointerup，坐标仍用 `canvas.getBoundingClientRect()`（board iframe 内，天然正确），无需主窗口参与。改造范围集中在 board 几处拖拽的 mousedown/mousemove/mouseup。
- **2026-07-21 Q2**：doc-editor 本地预生成 entry.id（`generateUUID` 纯函数提取为公共工具共享）。构造完整 entry（含 id）postMessage 给主窗口 push 到 databases，立即用该 id 包 source-marker，零等待。
- **2026-07-21 Q3**：采用 push 模式。doc-editor 编辑文档后防抖推送 `currentModule.document`，board 编辑便签后防抖推送 `currentModule.board`，主窗口副本始终最新，`scheduleSave` 零改动。**修正决策 4 表述**：document 和 board.notes 是共享状态（主窗口持有副本），"本地"指编辑权本地、持久化统一在主窗口，并非纯本地。
- **2026-07-21 Q4**：模块切换用广播附带数据。主窗口广播 `MODULE_CHANGED` 带 module 字段（document/board/databases），各 iframe 取所需部分整体替换并重渲染，零往返。单页/分屏统一处理（所有已加载 iframe 都收到）。
- **2026-07-21 Q5**：board iframe 内补 window resize 监听（RAF 防抖）调 `renderMinimap`。健壮性补丁，不分屏也该做（拖分割条/窗口缩放后鸟瞰图视窗框比例保持准确）。
- **2026-07-21 Q6**：入库后主窗口广播 `DB_ENTRY_ADDED`（带 entry），board 收到后刷新数据库面板；若"入库即落便签"开关开，同时落便签。
- **2026-07-21 Q7**：移除 board.html 内 `currentPage !== 'board'` 守卫，减少遗留假设。现有 `target.closest` 判断已足够防御非画布场景误触。
- **2026-07-21 Q8**：提取 `shared.js`（含 generateUUID、postMessage 通信桥、工具函数），index.html / doc-editor.html / board.html 都 `<script src>` 引用。标准做法，避免重复、便于维护。
- **2026-07-21 Q9**：单页模式也用 iframe 全宽加载对应页。主窗口始终是壳，根据模式决定加载哪个 iframe + 是否分屏布局，代码路径统一，避免两套渲染逻辑。
- **2026-07-21 Q10**：单页模式 navbar 提供双向页面切换（文档↔带团板）；分屏模式隐藏切换按钮（两个都显示无需切换）。现有"进入带团板"按钮（[L10423](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L10423)）改造为双向切换，分屏时隐藏。

### 复审问题决策（第二轮：交互元素边界）
- **2026-07-21 Q11**：保持右键菜单/选择器在 board iframe 内，依赖现有边界检测自动翻转展开方向。理由：iframe 分屏本身"左右各管各"，菜单不出栏是合理 UX；现有翻转逻辑已覆盖 90% 场景，实现成本最低。
- **2026-07-21 Q14**：分屏下「跳转至原文」不退出分屏，跨 iframe 定位左半边原文并高亮闪烁（滚动到视口 + source-flash 动画 + 面包屑提示）；单页模式仍切页。理由：分屏核心价值就是左右联动，跳转原文是典型联动场景。
- **2026-07-21 Q15**：分屏下点「编辑角色(角色图鉴)」→ 主窗口切到角色图鉴页（退出分屏显示），返回时回到分屏状态。与决策 7&8 一致。理由：角色图鉴内容复杂，全屏展示合理；返回保持上下文。
- **2026-07-21 Q17**：保持原生 `confirm()/prompt()`，接受居中整个 Electron 窗口。理由：功能正常、数量不多、用户习惯；不值得为视觉对齐大改。

### 第二轮已核实无问题的项（补充验证）
- **Q12 / Q21**：子菜单边界检测用 `window.innerWidth/Height`（[L22377](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L22377)/[L22389](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L22389)），即 iframe 视口，✅ 安全
- **Q19**：board 侧未实现 Ctrl+C/V 复制粘贴便签；现有「复制到/转移到」是跨流程单元操作（右键菜单内），✅ 无分屏问题
- **Q22**：连线模式事件在 board document 级监听，鼠标移出 board iframe 时会断；与 Q1 一并改造为 PointerEvent + setPointerCapture 后解决。

### 第二轮复审总结
共审查 13 个交互元素问题（Q11-Q23）：
- **需决策**：4 项（Q11/Q14/Q15/Q17）—— 全部确认
- **已核实安全**：9 项（Q12/Q13/Q16/Q18/Q19/Q20/Q21/Q22/Q23）

---

## 十一、带团板交互元素专项复审（2026-07-21）

### 审查范围
系统审查了带团板工具栏按钮、右键菜单、模态框、弹窗、快捷键、文本编辑、搜索栏、连线模式、跳转原文等所有交互元素在 iframe 分屏下的边界问题。

### 已确认安全（iframe 内正常工作）的元素
| 元素 | 挂载位置 | 定位 | 分屏影响 |
|---|---|---|---|
| 工具栏 (board-toolbar) | board 内 HTML | static/relative | ✅ 无影响 |
| 单元标题栏 (board-unit-header) | board 内 HTML | absolute/fixed 相对 board | ✅ 无影响 |
| 模式指示器 (board-mode-indicator) | board 内 HTML | absolute | ✅ 无影响 |
| 擦除模式气泡 (erase-tooltip) | board 内 HTML（[L10612](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L10612)） | 跟随鼠标 | ✅ viewport mousemove 更新，board 内安全 |
| 搜索栏 (board-search-bar) | board 内 HTML（[L10553](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L10553)） | flex 布局 | ✅ 无影响 |
| 鸟瞰图 (board-minimap) | board 内 HTML（[L10726](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L10726)） | fixed 相对 board iframe | ✅ 只在右半边，正确 |
| 缩略图 (board-thumbnails) | board 内 HTML（[L10623](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L10623)） | fixed 底部 | ✅ 无影响 |
| 世界时钟 (world-clock) | board 内 HTML（[L10628](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L10628)） | fixed | ✅ 归 board 栏，正确 |
| 数据库面板 (board-db-panel) | board 内 HTML | fixed 右侧 | ✅ 归 board 栏，正确 |
| 缩放指示器 (zoomIndicator) | board 内 HTML（[L10788](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L10788)） | fixed | ✅ 相对 board iframe，正确 |
| 空画布提示 (unit-empty-prompt) | canvas 内动态创建（[L18394](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L18394)） | absolute | ✅ canvas 内，正确 |

### 待决策的额外问题（第二轮：交互元素边界）
| # | 问题 | 现状详情 | 分屏下影响 |
|---|---|---|---|
| Q11 | 右键菜单 `document.body.appendChild` 超出 iframe 被裁切 | 便签右键菜单（[L20156](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L20156)）、纯文本块右键菜单（[L20156](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L20156)）、状态选择器（[L19137](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L19137)）、便签类型选择器（[L19729](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L19729)）都 `document.body.appendChild` + `position:fixed` + `e.clientX/Y` 定位 | 在 board iframe 靠近左边缘（分割线）或右边缘时，菜单弹出会被 iframe 边界裁切，看不到完整菜单 |
| Q12 | 右键菜单边界检测用 `window.innerWidth/Height`（iframe 视口）正确，但子菜单展开方向需确认 | 主菜单有边界检测（[L22338](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L22338)/[L20158](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L20158)），子菜单有 `_adjustSubmenusPosition()`（[L22352](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L22352)） | 需确认子菜单边界检测是否也用 `window.innerWidth/Height`（即 iframe 视口）。若是，则 iframe 内安全；若用了 document.body 尺寸则可能有问题 |
| Q13 | 编辑模态框 `document.body.appendChild`（board-edit-modal）被 iframe 限制 | 纯文本块编辑模态框（[L19986](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L19986)）`document.body.appendChild` + `position:fixed; inset:0; width:100%; height:100%`（[L7152](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L7152)） | ✅ 模态框相对 board iframe 全屏，在右栏内正常，无问题 |
| Q14 | 「跳转至原文」功能跨 iframe 失效 | `jumpToSource()`（[L22431](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L22431)）调用 `UIRender.switchPage('editor')` + `DocEditor.loadDocument()` 切到文档编辑器页并定位 source-marker | 分屏下文档编辑器就在左半边，不应切页，应改为**跨 iframe 定位到原文并高亮**，且分屏下点跳转不应退出分屏 |
| Q15 | 「编辑角色(角色图鉴)」跨 iframe 跳转 | 角色类型便签右键菜单首项改为"编辑角色(角色图鉴)"（[L22308](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L22308)），点击应跳角色图鉴 | 分屏下角色图鉴是全屏覆盖（决策7&8），点这个按钮应触发主窗口切到角色图鉴页（退出分屏显示），需 postMessage 通知主窗口 |
| Q16 | 快捷键焦点竞争（Ctrl+Z / Ctrl+F / Delete / 方向键 / Escape） | board 快捷键挂在 `document.keydown`（[L17513](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L17513)），doc-editor 也有自己的快捷键（[L14485](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L14485)） | ✅ iframe 隔离，两边 document 独立，快捷键不会互相干扰。焦点在哪个 iframe 哪个生效。**但需注意**：若焦点在主窗口 navbar/分割条上，两边快捷键都不生效（正常预期） |
| Q17 | 确认框/输入框 `confirm()`/`prompt()` 归属 | 删除便签用 `confirm()`（[L17550](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L17550)），新建单元可能用 prompt | `confirm()/prompt()` 是浏览器原生对话框，居中于整个 Electron 窗口（不受 iframe 限制）。✅ 不影响使用，但视觉上居中整个窗口而非右栏，略不协调 |
| Q18 | 文件导出/下载的 `<a download>` 与文件导入 input | `document.body.appendChild(a)` 触发下载（[L12307](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L12307)）模式在 board iframe 内 | `<a download>` 在 iframe 内也能正常触发下载（Electron 会弹出保存对话框），`<input type="file">` 同理。✅ 应正常工作 |
| Q19 | 复制粘贴便签（Ctrl+C/V） | board 侧**未实现** Ctrl+C/V 复制粘贴便签功能；现有复制仅在右键菜单「复制到/转移到其他单元」（跨流程单元，非剪贴板） | ✅ 无分屏问题 |
| Q20 | 颜色选择器 `<input type="color">` 弹出位置 | 纯文本编辑模态框里的颜色选择器（[L20005](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L20005)） | 系统原生颜色选择器，不受 iframe 限制。✅ 正常 |
| Q21 | 右键菜单「复制到/转移到其他单元」子菜单 | 子菜单在右键菜单内，悬停展开 | 子菜单展开方向 `_adjustSubmenusPosition()`（[L22352](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L22352)）需确认是否基于 iframe 视口边界检测。若已用 `window.innerWidth`，✅ 安全 |
| Q22 | 连线模式下从便签拖到画布边缘的事件 | 连线模式起点在便签，鼠标在画布内移动创建连线 | 连线是 SVG 在 canvas 内，鼠标移出 canvas 到工具栏/鸟瞰图区域是否断？现状：连线模式监听在 document 级？**需调查** |
| Q23 | Toast 提示位置 | `DocEditor.showToast` / `BoardManager.showToast` 挂在 document.body，fixed 定位 | Toast 在 board iframe 内居中。✅ 正常（右栏内提示，合理） |

---

## 十二、分割线拖动与画布交互专项复审（2026-07-21）

### 审查范围
系统审查了分割线拖动（board iframe resize）对 UI 元素的影响、画布内元素拖动、连线功能、撤回/重做功能在分屏 iframe 模式下的边界问题。

### 代码事实核实
- **resize 监听**：仅 [L17456-L17462](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L17456) 一处 window resize，RAF 防抖后只调 `_boRepositionAlertsBar()`（[L17460](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L17460)）。**未调 renderMinimap**（已记为 Q5）。
- **撤回栈结构**：BoardManager._undoStack（[L17388](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L17388)）是**操作记录式**（非快照），记录 addNote/deleteNote/moveNote/editNote/addConnection 等操作（[L18140](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L18140)）。最多 50 步（[L17390](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L17390)）。
- **撤回与连线联动**：deleteNote 撤回时保存 connectionsData 并恢复（[L18159](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L18159)）；addNote 撤回时清理关联连线（[L18146](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L18146)）。✅ 已联动。
- **便签删除联动**：`deleteNote`（[L21505](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L21505)）→ `_doDeleteNote`（[L21526](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L21526)）会清理关联连线（[L21535](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L21535)）+ 维护 placedEntryIds（[L21546](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L21546)）。✅ 完整。
- **连线模式是"点击式"非"拖拽式"**：点起点→点终点（[L22013 completeConnection](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L22013)），无 mousemove 跟随临时连线。鼠标移出 iframe 不影响（无持续拖拽状态）。✅ **修正 Q22 判断**。
- **确认框有两套**：大部分用 `App.showConfirm`（自定义模态 `confirmOverlay`，[L11127](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L11127)，`position:fixed; inset:0`，[L2065](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L2065)）；少数用原生 `confirm()`（删除选中便签 [L17550](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L17550)、导入战斗部署 [L27612](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L27612)、删除角色 [L29444](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L29444)）和 `prompt()`（物品/法术名称输入 [L16815](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L16815)）。

### 待决策的额外问题（第三轮：分割线拖动与画布交互）
| # | 问题 | 现状详情 | 分屏下影响 |
|---|---|---|---|
| Q24 | 分割线拖动期间（实时 resize）UI 抖动 | 拖动分割条 = board iframe 宽度持续变化 = window resize 高频触发。现有 resize 只重定位 alerts-bar（RAF 防抖），其他 fixed 元素（鸟瞰图/缩略图/时钟/数据库面板/缩放指示器）用 CSS fixed 定位会自动适应，但鸟瞰图视窗框比例不会重算（Q5 已定补 renderMinimap） | 拖动期间鸟瞰图视窗框比例失真，松手后才正确（若 Q5 实现了 resize→renderMinimap）。拖动期间其他 fixed 元素因 CSS 自适应无问题 |
| Q25 | 分割线拖动时画布内便签/连线视觉错位 | 便签位置存虚拟坐标（unit.notes[].x/y），renderNote 时用 `left/top: x/y px`。canvas 是 `position:relative`，便签 absolute。iframe resize 后 canvas 尺寸变，但便签 left/top 不变（虚拟坐标），**视觉位置正确**（相对 canvas 左上角）。连线 SVG 基于 canvas 尺寸重算（renderConnections） | ✅ 便签位置正确。连线需 renderConnections 重算，但 resize 未触发 renderConnections，拖动期间连线端点可能短暂错位，松手后（若补 resize→renderConnections）正确 |
| Q26 | App.showConfirm 的 confirmOverlay 归属 | `confirmOverlay`（[L11127](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L11127)）是主窗口 index.html 的静态 HTML 元素，`App.showConfirm`（[L13041](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L13041)）操作它。board.html 拆分后，board iframe 内调用 `App.showConfirm`（如删除锁定便签 [L21516](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L21516)）会找不到元素 | board 内的确认框功能失效（删锁定便签、删除自定义类型等）。需确认框归属方案 |
| Q27 | 撤回/重做后状态同步 | board 撤回/重做操作 unit.notes/connections 后，调 saveBoard + StorageManager.scheduleSave（[L21557](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L21557)）。拆分后 saveBoard 写本地、scheduleSave 在主窗口 | 撤回/重做后需推送 currentModule.board 给主窗口（Q3 push 模式）。撤回不涉及数据库/document，无需广播其他 iframe。✅ 按 Q3 机制处理即可 |
| Q28 | 撤回栈是否跨流程单元 | 撤回栈记录 unitIndex（[L18134](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L18134)），操作可跨单元。切流程单元不清理撤回栈 | ✅ 分屏不影响（board 内部逻辑，无跨 iframe） |
| Q29 | 连线模式跨 iframe 鼠标移出 | 连线是点击式（点起点→点终点），无持续 mousemove 拖拽。鼠标移出 board iframe 到左半边时，连线模式状态保留（等待点击终点），回到 board 点终点即可继续 | ✅ 无问题（修正原 Q22 判断）。连线模式不需要 setPointerCapture |
| Q30 | 框选时鼠标移出 iframe | 框选是 mousedown→mousemove→mouseup 持续拖拽（[L17654](file:///c:/Users/沉云/Desktop/备团助手/electron-app/index.html#L17654)），靠 document mousemove/mouseup。鼠标移出 board iframe 到左半边，mouseup 丢失 | 框选卡住。已由 Q1（setPointerCapture）统一解决 |

### 复审问题决策（第三轮：分割线拖动与画布交互）
- **2026-07-21 Q24**：resize 回调 RAF 防抖调 `renderMinimap` + `renderConnections`，拖动时每帧重算，鸟瞰图/连线实时跟随。理由：鸟瞰图重算开销小，实时跟随体验好；是 Q5（resize→renderMinimap）的自然延伸，并补充 renderConnections。
- **2026-07-21 Q27**：撤回/重做后按 Q3 push 模式处理——推送 `currentModule.board` 给主窗口。撤回不涉及数据库/document，无需广播其他 iframe。
- **2026-07-21 Q28**：撤回栈跨流程单元是 board 内部逻辑，分屏不影响，无需额外处理。
- **2026-07-21 Q29**：连线模式是点击式（点起点→点终点），无持续拖拽，鼠标移出 board iframe 不影响。**修正原 Q22 判断**：连线模式不需要 setPointerCapture。
- **2026-07-21 Q30**：框选鼠标移出 iframe 已由 Q1（setPointerCapture）统一解决。
- **2026-07-21 Q25**：无需单独决策，由 Q24 覆盖。便签位置存虚拟坐标用 left/top 定位，iframe resize 后位置正确；连线由 Q24 的 resize→renderConnections 每帧重算，不错位。
- **2026-07-21 Q26**：各 iframe 自带 `confirmOverlay` 副本（HTML+CSS 复制到 board.html/doc-editor.html），`showConfirm/hideConfirm/confirmOk` 逻辑放 shared.js 共享。理由：确认框结构简单复制成本低、独立无跨 iframe 通信、确认框在对应栏内显示视觉合理。

### 第三轮复审总结
共审查 7 个分割线拖动与画布交互问题（Q24-Q30）：
- **需决策**：3 项（Q24/Q25/Q26）—— 全部确认（Q25 由 Q24 覆盖）
- **已有决策覆盖**：4 项（Q27/Q28/Q29/Q30）—— 分别由 Q3/Q1 覆盖或核实安全
- **重要修正**：Q29 修正了原 Q22 的错误判断（连线是点击式，不需要 setPointerCapture）

---

## 十三、落地进展记录

> 记录实际实现进度，按阶段推进。

### 文件清单
- `index.html`：单页模式主入口（原完整应用，新增"分屏模式"按钮）
- `split-screen.html`：分屏壳（左右 iframe + 分割条 + 顶部工具栏）
- `board.html`：带团板 iframe 页（基于 index.html 副本，默认显示带团板）
- `doc-editor.html`：文档编辑器 iframe 页（基于 index.html 副本，默认显示编辑器）
- `shared.js`：共享通信桥 + 工具函数
- `index.html.bak`：原始 index.html 备份

### 阶段一：坐标系统健壮化 ✅ 已完成
**目标**：为 iframe 分屏后拖拽不丢指针、坐标不错位。

**改动清单**：
1. **拖拽交互全部从 MouseEvent → PointerEvent + setPointerCapture：
   - 画布平移（右键/空格+左键）：`pointerdown 对 `boardViewport` setPointerCapture
   - 框选（左键空白）：`pointerdown` 对 canvas setPointerCapture
   - 便签拖拽：`startNoteDrag` 对便签元素 setPointerCapture
   - 便签缩放（resize handle）：`startResize` 对便签元素 setPointerCapture
   - 缩略图栏拖拽滚动：thumbBar pointerdown setPointerCapture
2. **新增 `_activePointerTarget / _activePointerId** 追踪当前捕获元素
3. **新增 `pointercancel` 监听**：与 pointerup 走同一清理路径
4. **window resize 监听**（RAF 防抖）：自动重绘 `renderMinimap()` + `renderConnections(currentUnitIndex)`

**涉及文件**：`index.html`（同步到 board.html 也有）

### 阶段二：页面拆分 ✅ 已完成
**目标**：主窗口变分屏壳，文档编辑器和带团板各跑独立 iframe。

**改动清单**：
1. **board.html**：复制 index.html，默认 `currentPage = 'board'`，隐藏 navbar/进入带团板按钮/返回编辑器按钮，`_isBoardIframe = true`
2. **doc-editor.html**：复制 index.html，默认 `currentPage = 'editor'`，隐藏 navbar/进入带团板按钮，`_isDocIframe = true`
3. **split-screen.html**：分屏壳页面
   - 顶部工具栏（"分屏模式"标题 + 退出分屏按钮）
   - 左右 flex 布局 + 4px 分割条（可拖拽，min-width 200px）
   - `SplitShell` 控制器：分割条拖拽、消息转发、退出分屏
4. **index.html**：恢复为单页模式入口，新增"分屏模式"按钮（`App.enterSplitScreen()`）
5. **shared.js**：SharedBridge 通信桥（on/send/handleMessage/isInIframe/generateUUID/escapeHtml）

### 阶段三：跨 iframe 通信桥 + 状态同步 ✅ 已完成
**目标**：模块切换等关键状态在两个 iframe 间同步。

**改动清单**：
1. **SharedBridge**：postMessage 封装，`on(type, fn)` 订阅、`send(type, payload)` 发送
2. **模块切换双向同步**：
   - doc-editor / board 任一侧切换模块 → `MODULE_CHANGED` 消息 → 另一侧接收后切换
   - 入口：`App.openModule()` 中发送，`App.init()` 中监听
3. **主窗口转发**：SplitShell 中继 MODULE_CHANGED、ENTRY_ADDED、JUMP_TO_SOURCE 等消息

### 阶段四：工作流简化 ✅ 已完成
**目标**：文档编辑器入库即落在带团板上生成便签。

**改动清单**：
1. **入库即落便签**（ENTRY_ADDED）：
   - 发送方：`DocEditor.addToDatabase()` 末尾，入库成功后发送 `ENTRY_ADDED`（含 dbKey、entryId、entry）
   - 接收方：`BoardManager.init()` 中监听 ENTRY_ADDED，调用 `placeFromDatabase(dbKey, entryId)`
   - 主窗口：SplitShell 转发 doc → board
2. **跳转原文跨 iframe 定位**（JUMP_TO_SOURCE）：
   - 发送方：`BoardManager.jumpToSource()` 分屏模式下发送消息并 return（不切页面）
   - 接收方：doc-editor App.init 中监听，找 `.source-marker` 滚动 + 高亮闪现；fallback 文本搜索
   - 主窗口：SplitShell 转发 board → doc

### 阶段五：边界完善 ⏳ 进行中
**已完成**：
- ✅ 分割条拖动 → resize → 重绘 minimap/connections（阶段一已覆盖 Q24/Q5）
- ✅ 模块切换双向同步（阶段三已覆盖 Q3）
- ✅ 入库即落便签（阶段四）
- ✅ 跳转原文跨 iframe 定位（阶段四延伸）
- ✅ 退出分屏按钮（跳回 index.html 单页模式）
- ✅ 隐藏 iframe 内不需要的导航按钮（返回编辑器/进入带团板）
- ✅ 撤回/重做：board 内部独立撤回栈，分屏不影响（Q28 已确认）
- ✅ 连线模式：点击式，跨 iframe 不影响（Q29 已确认）
- ✅ 世界时钟：position: absolute 相对带团板视口，分屏下自然在右半边（Q25 延伸）

**待后续迭代**：
- ⏳ 角色图鉴/战斗模块全屏覆盖（Q20/Q21）—— 当前点击角色图鉴会在 board iframe 内显示（只占右半边），需改为主窗口层全屏 overlay
- ⏳ 分屏模式下的撤回/重做数据持久化一致性
- ⏳ 多库组切换同步
- ⏳ 分屏比例记忆（用户偏好持久化）

### 已知限制 / 待验证
1. **electronAPI 在 iframe 中是否可用**：需验证 Electron 中 iframe 是否继承 preload 的 `window.electronAPI`。如不可用，StorageManager 会走 localStorage 兜底，数据仍可读写但不持久化到磁盘文件。
2. **数据一致性**：当前两个 iframe 各自独立运行 StorageManager，若同时写数据可能有竞争。后续应将主窗口设为真相源，iframe 走代理模式。
3. **性能**：两个完整应用实例内存翻倍，低配机器可能有影响。
