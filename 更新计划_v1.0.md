# 备团助手 v1.0 更新计划

> 最后更新：2026-07-12
> 状态：实现中

---

## 数据来源

三个规则书文件夹，共 1123 个 .htm 文件（GBK 编码）：

| 规则书 | 文件数 | 内容 |
|--------|--------|------|
| 玩家手册2024 | 218 | 职业/种族/背景/法术/专长/装备/规则 |
| 城主指南2024 | 305 | 魔法物品/宝藏/据点/位面/带团技巧 |
| 怪物图鉴2025 | 600 | 500+怪物数据卡 |

存储方式：独立 JSON 文件（外部文件，非打包进 exe），工具启动时加载到内存。

---

## 一、5R 规则书装载与搜索

### 1.1 规则书数据翻译

将三个文件夹的 .htm 文件解析为统一 JSON，按类型分类：

| 类型 | 数量 | 解析难度 | 方式 |
|------|------|----------|------|
| 法术 | ~500 | 低 | 自动解析固定格式（名称/学派/环位/施法时间/距离/成分/持续时间/效果/升环） |
| 怪物 | ~500+ | 低 | 自动解析 stat-block 结构（AC/HP/速度/六属性/技能/感官/语言/CR/特质/动作/传奇动作） |
| 武器/护甲/装备 | ~200 | 低 | 自动解析表格（名称/伤害/属性/精通/重量/价格） |
| 魔法物品 | ~200 | 中 | 自动解析（分布在城主指南各子文件夹） |
| 专长 | ~100 | 中 | 半自动解析（名称/先决条件/效果） |
| 职业特性/子职 | ~120 | 中高 | 部分手动辅助 |
| 规则说明 | ~200 | — | 保留原文 HTML/纯文本，全文搜索时呈现完整页面内容 |

存储方式：外部 `dnd5r_rules.json`，工具启动时异步加载。

### 1.1 HTML清洗方法论（已确立的成熟套路）

解析脚本 `parse_rules.js` 中的 `cleanHtml()` 函数是核心，所有规则说明类条目的正文均经过此函数处理。以下为完整的处理流水线，可直接复用于后续规则书内容扩展：

#### 第一步：编码预处理
```
原始 .htm 文件（GBK/GB2312 编码）→ iconv-lite.decode(buffer, 'gbk') → UTF-8 字符串
```
- 优先尝试 `gbk`，失败回退 `gb2312`，最后 fallback 到 `utf-8`
- 依赖包：`iconv-lite`

#### 第二步：去除 script/style 标签
```javascript
result.replace(/<script[^>]*>[\s\S]*?<\/script>/gi, '');
result.replace(/<style[^>]*>[\s\S]*?<\/style>/gi, '');
```

#### 第三步：格式标签标准化（在执行表格提取之前，确保表格内也得到转换）

| 原始标签 | 转换为 | 说明 |
|----------|--------|------|
| `<FONT color=#XXX>` | `<span style="color:#XXX">` | 颜色保留 |
| `<FONT size=N>` | `<span style="font-size:Xem">` | 字号映射：1→0.67em, 2→0.83em, 3→1em, 4→1.15em, 5→1.38em, 6→1.83em, 7→2.5em |
| `<FONT color=#XXX size=N>` | `<span style="color:#XXX;font-size:Xem">` | 同时含颜色和字号，合并 style |
| `<STRONG>`, `<B>` | `<strong>` | 加粗标准化 |
| `<I>`, `<EM>` | `<em>` | 斜体标准化 |
| `</FONT>` | `</span>` | 统一闭合 |

#### 第四步：保存表格块
```javascript
// 将 <TABLE>...</TABLE> 整体替换为占位符 __TABLE_BLOCK_N__，避免后续清理破坏表格结构
result.replace(/<TABLE[^>]*>[\s\S]*?<\/TABLE>/gi, match => '__TABLE_BLOCK_' + idx + '__');
```

#### 第五步：标签分类处理

**块级标签 → 换行符 `\n`**：
`P, DIV, H1-H6, UL, OL, BLOCKQUOTE, PRE, DL, DT, DD, HEADER, SECTION, ARTICLE, FOOTER, BR, HR`

**列表项标签 → 换行符 + 项目符号前缀**：
```
<LI> → \n· 
</LI> → \n
```
注意：`LI` 不放入块级标签组，需独立处理以添加 `· ` 前缀。

**行内/装饰标签 → 直接删除**（不产生换行）：
所有未在以上规则中处理的标签（如 `<A>`, `<U>`, `<META>`, `<CENTER>` 等），使用负向先行断言删除：
```javascript
const keepTags = 'strong|em|span|table|thead|tbody|tr|td|th|caption|colgroup|col';
result.replace(new RegExp('</?(?!' + keepTags + ')\\b\\w+[^>]*>', 'gi'), '');
```
此正则会保留已标准化的 `<strong>`, `<em>`, `<span>` 以及表格相关标签。

#### 第六步：实体转义
```javascript
&nbsp; → 空格
&amp;  → &
&lt;   → <
&gt;   → >
```

#### 第七步：还原表格块
```javascript
// 将占位符替换回标准化后的表格 HTML，并清除表格内部的换行符
tableHtml.replace(/<TABLE[^>]*>/gi, '<table class="rulebook-table">');
tableHtml.replace(/<\/TABLE>/gi, '</table>');
// ... TR/TD/TH 同理转为小写 ...
tableHtml.replace(/\n/g, '');  // 关键：清除表格内换行，防止浏览器将 <br> 推到表格上方
result.replace('__TABLE_BLOCK_' + i + '__', tableHtml);
```

#### 第八步：空白规范化
```javascript
// 1. 合并孤立的 · 列表符号与其后内容（防止 · 独自占一行）
result.replace(/(\u00b7)\s*\n\s*/g, '$1 ');
// 2. 压缩水平空白
result.replace(/[ \t]+/g, ' ');
// 3. 三个及以上连续换行 → 单个空行
result.replace(/\n{3,}/g, '\n\n');
// 4. 去除首尾空白
result.replace(/^\s+|\s+$/g, '');
```

#### 详情窗口渲染侧
在 `index.html` 中渲染 `description` 时：
```javascript
// 换行符 → <br>，不转义 HTML（让表格和格式标签生效）
detailHtml += `<div class="rulebook-detail-desc">${item.description.replace(/\n/g, '<br>')}</div>`;
```

配套 CSS 保留表格样式和格式标签渲染：
```css
.rulebook-detail-desc { white-space: pre-wrap; }  /* 保留空白语义 */
.rulebook-table { width:100%; border-collapse:collapse; }  /* 表格基础样式 */
.rulebook-table th { background:var(--accent); color:white; }  /* 表头 */
.rulebook-table td { border:1px solid var(--border); }  /* 单元格边框 */
.rulebook-table tr:nth-child(even) { background:var(--card); }  /* 斑马纹 */
```

### 1.2 规则书搜索集成

- 搜索范围：文档内容 + 带团板便签 + 规则书条目（三类数据源）
- 搜索结果分组展示：
  - 📄 文档匹配
  - 📌 带团板便签匹配
  - 📖 规则书条目（按子类别分组：法术/怪物/物品/专长/规则说明等）
- 点击规则书条目 → 弹出详情窗口，搜索结果列表不消失，支持连续查阅多个条目
- 规则说明类条目：搜索命中后直接展示对应全文页面
- 编辑器增加"在规则书中搜索"按钮，选中文本后一键搜索

---

## 二、角色详情系统（两层结构）

### 2.1 第一层：鼠标悬停 → 角色速览卡

鼠标悬停在带团板角色/怪物便签上 300ms 后，浮出紧凑信息卡：

- 显示：名称、CR、阵营、体型、AC、HP条（当前/最大）、核心六属性、环位消耗点阵、特性剩余次数摘要、物品/法术数量
- 即时反映动态数据（HP消耗、环位消耗等）
- 纯展示，不可编辑
- 底部提示"双击查看完整详情"
- 智能定位：优先在便签右侧弹出，空间不足时自动调整到左侧/上方/下方，避免遮挡相邻便签

### 2.2 第二层：双击 → 角色图鉴页（独立页面）

双击任意角色/怪物便签 → 切换到独立的"角色图鉴"页面。类似于原神角色界面：左侧角色列表，右侧选中角色详情。

```
┌──────────────────────────────────────────────────────────┐
│  ← 返回带团板             角色图鉴 · [当前模组名]        │
├──────────────┬───────────────────────────────────────────┤
│              │                                           │
│ 🔍 搜索角色  │  布鲁诺·战锤  LV5 矮人战士                 │
│ （含规则书） │                                           │
│              │  ┌─────────────────────────────────────┐ │
│ 👤 玩家角色  │  │ [属性] [物品] [法术] [特性] [监控]  │ │
│  ● 布鲁诺   │  ├─────────────────────────────────────┤ │
│  ○ 艾莉丝   │  │                                     │ │
│  ○ 塞德里克 │  │  当前标签页的完整可编辑内容            │ │
│              │  │                                     │ │
│ 👹 怪物/NPC  │  │  属性页：AC/HP/六属性/技能/感官/     │ │
│  ○ 成年红龙 │  │  阵营/体型/语言等（现有表单增强）     │ │
│  ○ 地精×3   │  │  物品页：物品清单 + 添加/删除         │ │
│  ○ 骷髅守卫 │  │  法术页：法术清单 + 添加/删除         │ │
│              │  │  特性页：特性清单 + 添加/删除         │ │
│ [+ 新建角色] │  │  监控页：环位追踪 + 特性次数追踪      │ │
│              │  │                                     │ │
│              │  │                         [保存修改]  │ │
│              │  └─────────────────────────────────────┘ │
└──────────────┴───────────────────────────────────────────┘
```

**关键设计：**

- 角色数据来源：当前模组的带团板上所有角色/怪物便签（每个模组角色库独立）
- 左侧列表分两组：👤 玩家角色 / 👹 怪物&NPC
- 搜索框搜索范围：当前模组已有角色 + 规则书参考条目（搜"龙"不仅显示已有的龙，还会显示规则书里所有龙类怪物作为参考，点击可查看规则书条目详情）
- 点击列表中的角色 → 右侧即时切换详情（无需打开/关闭）
- 右侧详情**完全可编辑**（所有标签页内容均可修改）
- "[+ 新建角色]" → 仅需输入角色名称即可创建空白骨架 → 自动在带团板上生成对应的角色便签，后续在图鉴内逐步完善属性
- 带团板上删除角色便签 → 图鉴列表中同步移除

### 2.3 标签页内容

```
[属性] [物品] [法术] [特性] [监控]
```

- **属性页**：AC、HP、先攻、速度、六属性、技能、感官、语言、阵营、体型、CR、特质文本、动作文本（增强现有 characterData 表单）
- **物品页**：角色拥有的物品清单。可从规则书添加或自定义输入。每件物品显示名称、数量、描述
- **法术页**：角色已知/已准备的法术清单。可从规则书添加（自动填充学派/环位/施法时间等）或自定义输入
- **特性页**：职业特性、种族特性等。可从规则书添加或自定义输入
- **监控页**：环位消耗追踪面板 + 特性使用次数追踪面板（详见第五章）

### 2.4 添加物品和法术

两种方式：
- **从规则书选**：搜索 → 选择 → 自动填充完整详情（名称、描述、属性等）
- **自定义输入**：手动填写名称和描述，适用于自制物品/法术

---

## 三、物品数据类型增强

- 物品便签/条目新增 `ruleSourceId`（关联规则书物品）
- 创建/编辑物品时可"从规则书套用"属性（名称、类别、稀有度、描述等）
- 物品便签卡片展示更丰富信息（类别标签、稀有度颜色标识）

---

## 四、战斗轮行动引导

### 4.1 技能卡片式行动展示（简化版）

由于角色可能拥有大量法术/特性/动作，卡片设计为**紧凑格式**：仅显示名称 + 一个核心数值标签。鼠标悬停时在旁边浮出完整详情卡。

#### 布局

五列分组排列：

```
┌─ ⚔️ 攻击 ─────────────┐ ┌─ 🔥 特性 ─────────────┐ ┌─ 🧪 法术 ─────────────┐
│ 撕咬    +14 2d10+8   │ │ 火焰吐息  DC21 充能   │ │ 灼热射线   2环        │
│ 爪击    +14 2d6+8    │ │ 传奇抗性  2/3次       │ │ 命令术     1环        │
│ 尾击    +14 2d8+8    │ │ 多重攻击              │ │ 侦测魔法   1环        │
│                       │ │ 施法（龙类天生）      │ │ 火球术     3环·1/日   │
└───────────────────────┘ └───────────────────────┘ └───────────────────────┘

┌─ 🏃 动作 ─────────────┐ ┌─ 💰 道具 ─────────────┐
│ 攻击（Attack）        │ │ 治疗药水     ×2      │
│ 疾走（Dash）          │ │ 抗毒剂       ×1      │
│ 撤离（Disengage）     │ │                       │
│ 闪避（Dodge）         │ │                       │
│ 协助（Help）          │ │                       │
│ 躲藏（Hide）          │ │                       │
│ 搜索（Search）        │ │                       │
│ 预备（Ready）         │ │                       │
└───────────────────────┘ └───────────────────────┘
```

**🏃 动作区域**固定包含 8 个 D&D 通用基础动作，所有角色始终显示。

#### 卡片颜色

| 类别 | 边框/光晕色 | 含义 |
|------|------------|------|
| ⚔️ 攻击 | 暗红 `#c0392b` | 侵略性 |
| 🏃 动作 | 灰蓝 `#7f8c8d` | 战术性 |
| 🔥 特性 | 暗金 `#d4a017` | 传奇感 |
| 🧪 法术 | 深蓝紫 `#2c3e80` | 魔法感 |
| 💰 道具 | 暗绿 `#27ae60` | 实物感 |

#### 悬停详情卡

鼠标悬停任意卡片 → 在旁边浮出紧凑详情卡，显示该行动的完整信息：

- 攻击卡片：命中加值、伤害骰、伤害类型、射程、附加效果
- 法术卡片：环位、学派、施法时间、距离、成分、持续时间、完整效果描述
- 特性卡片：使用次数、恢复时机、完整效果描述
- 道具卡片：数量、效果描述

#### 呼吸动效

- **静态呼吸**：所有卡片在空闲时，以 3-4 秒周期做极微弱的缩放脉冲（`scale 1.0→1.015→1.0`），配合边框光晕明暗呼吸。整体面板的金色边框有持续光晕流动效果（D&D 书页金边）
- **动态呼吸**：鼠标移入某列区域 → 该列卡片集体微上浮（`translateY -2px`），光晕增强；移出后缓慢回落

纯 CSS `animation` + `transition` 实现，无 JS 动画循环。

#### 入场动画

行动面板在战斗覆盖层内，当轮到该角色时自动弹出，回合结束后关闭。卡片交错入场：
1. 状态提醒条先出现（红色警示闪烁）
2. 五个分类列依次从下方弹出入场（每列间隔 80ms），带弹跳缓动
3. 每列内卡片从上到下依次淡入（每张间隔 50ms）
4. 总持续时间约 0.5 秒

### 4.2 状态异常提醒

```
╔══════════════════════════════════════════════════════════╗
║ ⚠️ 状态异常提醒                         [✓ 全部确认]    ║
╠══════════════════════════════════════════════════════════╣
║                                                          ║
║ 🔴 魅惑：每回合结束时进行 DC15 感知豁免，  [✓ 确认]     ║
║    成功则状态结束                                        ║
║                                                          ║
║ 🔴 目盲：施法者回合结束时进行 DC12 体质豁免，[✓ 确认]   ║
║    成功则状态结束                                        ║
║                                                          ║
╚══════════════════════════════════════════════════════════╝
```

交互规则：
- 每条提醒右侧有独立 **[确认]** 按钮
- 右上角 **[全部确认]** 一键确认所有提醒
- **该角色回合结束后**，所有提醒整体淡出消失（0.3秒过渡），无论是否已确认
- 已确认的条目变灰/半透明，未确认的保持高亮红色

---

## 五、角色资源监控

### 5.1 环位消耗追踪

**第一版方案（当前可实现）**：
- GM 手动设置各环位（1~9环）的最大环位数
- 怪物角色如果关联规则书数据卡，自动从其天生施法能力中提取环位信息作为默认值
- 施法消耗，长休恢复全部
- 角色便签卡片上：环位显示为点阵（实心=已消耗，空心=可用）
- 角色图鉴监控页：各环位列显示"已消耗/最大"，支持 +/- 手动调整
- 每个角色独立开关

**后续版本（待职业升级表数据解析完成后）**：
- GM 输入角色的职业 + 等级（含兼职情况）
- 自动查询规则书中的职业升级表，计算环位最大值
- GM 保留手动修改权

### 5.2 特性使用次数追踪

- 有次数限制的特性（如"传奇抗性 3次/日"）可追踪剩余次数
- 显示格式：`传奇抗性 · 2/3 [-][+]`
- 长休：恢复全部"每日恢复"特性
- 短休：恢复"短休恢复"特性（依赖规则书数据中的恢复时机字段）
- GM 可手动增减
- 每个角色独立开关

### 5.3 恢复触发

- 提供"短休"/"长休"按钮
- 短休：恢复短休特性 + 部分职业资源
- 长休：恢复全部环位 + 全部特性 + HP回满

---

## 工作量参考

| 步骤 | 占比 | 说明 |
|------|------|------|
| A. 规则书解析脚本 | 30% | 核心工作量，自动化解析 1123 个文件 |
| B. 规则书加载 + 搜索索引 + 搜索结果混合展示 | 15% | 搜索引擎 + 分组 UI |
| C. 角色图鉴页 + 两层角色详情 | 15% | 全新独立页面 + 悬停速览卡 |
| D. 战斗行动面板（技能卡片 + 呼吸动效） | 20% | 卡片 UI + 悬停详情 + 动画 |
| E. 资源监控（环位/特性 + 休息恢复） | 15% | 数据模型 + UI + 恢复逻辑 |
| F. 状态豁免提示 | 5% | 确认交互 + 回合清除 |
